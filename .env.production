# 生产环境配置
# 此文件仅在执行 yarn build 时使用

# API 基础地址
# 方案1：使用相对路径（推荐，通过 Nginx 代理）
# 如果 Nginx 配置了 /api 代理到后端，使用此配置
REACT_APP_BASE_URL=/api/v1

# 方案2：直接访问后端（如果 Nginx 未配置代理，使用此配置）
# REACT_APP_BASE_URL=http://192.168.31.150:8000/api/v1

# 静态资源地址（可选）
# 如果使用 CDN 或静态资源服务器，配置此地址
# 例如：REACT_APP_ASSETS_BUCKET=https://cdn.example.com
REACT_APP_ASSETS_BUCKET=
